export const LipRendererConfig = {
  featherRadius: 2,
  defaultOpacity: 0.75,
  renderFPS: 30,
  minimumCaptureQuality: 80, // We can also rely on captureReady
  enableSpecularHighlights: true,
  smoothingAlpha: 0.7 // Exponential smoothing factor for landmarks
};
